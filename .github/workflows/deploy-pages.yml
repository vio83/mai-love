# ============================================================
# VIO 83 AI ORCHESTRA — GitHub Pages deployment
# NOTE: Pages is deployed directly from branch main, folder /docs
# This workflow is intentionally disabled.
# Go to Settings > Pages > Source: "Deploy from a branch"
# Branch: main, Folder: /docs
# ============================================================
# If you want to re-enable automated deployment via Actions,
# change the Pages source to "GitHub Actions" in repo settings.

name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "GitHub Pages deploys automatically from branch main, folder /docs."
          echo "No workflow needed. Go to Settings > Pages to configure."
