# VIO 83 AI ORCHESTRA — Dipendenze Python
# Installare con: pip install -r requirements.txt
#
# NOTA: Testato con Python 3.11 / 3.12 / 3.13
# Python 3.14 non supporta chromadb/litellm (C-extensions non ancora compilate)

# === Core Server ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# === HTTP Client (per chiamate Ollama/API) ===
# httpx senza SOCKS proxy per evitare conflitti con environment locale
httpx[http2]>=0.27.0

# === Database ===
# SQLite built-in — nessuna dipendenza extra richiesta

# === Knowledge Base — Document Ingestion ===
pypdf>=4.0.0                 # Fallback leggero per PDF
python-docx>=1.1.0           # DOCX extraction

# === Knowledge Base — Embedding e Vector Search ===
# Integrate in standard install per esperienza out-of-box su Python supportato.
# Su Python 3.14 restano esclusi via marker (fallback SQLite FTS5 resta attivo).
chromadb>=0.5.0,<1.0.0; python_version < "3.14"
sentence-transformers>=3.0.0; python_version < "3.14"

# === Rate Limiting (opzionale — già implementato natively in server.py) ===
# slowapi>=0.1.9             # Alternativa basata su limits

# === Metriche e Osservabilità (opzionale) ===
# prometheus-client>=0.21.0  # Metriche Prometheus
